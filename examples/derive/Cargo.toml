[package]
name = "derive"
version = "0.1.0"
edition = "2024"

[dependencies]
persistent_config = { path = "../../persistent_config", features = ["derive"] }
serde = { version = "1.0.219", features = ["derive"] }
